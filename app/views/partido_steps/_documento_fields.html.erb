<%= f.input :descripcion %>

<% if f.object.archivo %>
    <%= link_to (image_tag f.object.archivo.url(:thumb)), f.object.archivo.url  %>
<% end %>

<%= f.input :archivo, as: :file %>

<%= link_to_remove_association "mark for remove document", f %>
