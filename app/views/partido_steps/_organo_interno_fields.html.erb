<div class="col-md-4">
  <div class="panel panel-default">
    <div class="panel-heading">
      <% if f.object.id.nil? %>
        Nuevo órgano
      <% else %>
        <%= f.input :nombre %>
      <% end %>
    </div>
    <div class="panel-body">

      <% if f.object.id.nil? %>
        <%= f.input :nombre  %>

      <% end %>

      <%= f.input :funciones, as: :text, :input_html => { :cols => 150, :rows => 15 } %>
      <%= f.input :contacto  %>
      <%= f.input :num_integrantes  %>

    </div>
    <div class="panel-footer org-interno">
      <%= link_to_remove_association f, class: 'btn btn-danger checkForDelete' do %>
        <i class="fa fa-trash"></i> Marcar para eliminar
      <% end %>
    </div>
  </div>
</div>
