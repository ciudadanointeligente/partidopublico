<div class="container">
  <div class="row">
    <div class="col-md-3" id="row">
      <%= datagrid_form_for @grid, :method => :get, :url => wizard_path %>
    </div>
    <div class="col-md-9">
      <%= paginate(@grid.assets) %>
      <%= datagrid_table @grid %>
      <div class='total'>Total: <%= @grid.assets.total_count %></div>
      <%= paginate(@grid.assets) %>
    </div>
  </div>
</div>
