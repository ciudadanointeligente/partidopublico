<%= render :layout => 'form' do |f| %>

<%= f.fields_for :eleccion_populars do |builder| %>
    <%= render 'eleccion_popular_fields', :f => builder %>
<% end %>
<%= link_to_add_association 'add eleccion popular', f, :eleccion_populars %>

<% end %>