<!-- <% @datos_cargos.each do |r| %>
  <% if r['cargos'].any? %>
  <h3><%= r['region'] %></h3>
  <table class="table">
    <thead>
      <tr>
        <th>Comuna</th>
        <th>Persona</th>
        <th>Cargo</th>
      </tr>
    </thead>
    <tbody>
      <% r['cargos'].each do |s| %>
      <tr>
        <td><%= s['comuna'] %></td>
        <td><%= s['persona'] %></td>
        <td><%= s['cargo'] %></td>
      </tr>
      <% end %>
    </tbody>
  </table>
  <% end %>
<% end %> -->
<%= form_tag partido_autoridades_path, :method =>  :get do |f| %>
  <%= text_field_tag :nombre %>
  <%= submit_tag 'Buscar' %>
<% end %>

<% if @autoridades.any? %>
  <div class="row">

    <h3>Autoridades Designadas</h3>

    <table class="table">
      <thead>
        <tr>
          <th>Título</th>
          <th>Nombre</th>
          <th>link</th>
        </tr>
      </thead>
      <tbody>
        <% @autoridades.each do |c| %>
        <tr>
          <td><%= c.titulo %></td>
          <td><%= c.persona.nombre + " " + c.apellidos %></td>
          <td><%= c.intereses %></td>
        </tr>
        <% end %>
      </tbody>
    </table>
  </div>
<% end %>
