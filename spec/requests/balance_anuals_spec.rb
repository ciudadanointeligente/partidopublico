require 'rails_helper'

RSpec.describe "BalanceAnuals", type: :request do
  describe "GET /balance_anuals" do
    xit "works! (now write some real specs)" do
      get balance_anuals_path
      expect(response).to have_http_status(200)
    end
  end
end
