require 'rails_helper'

RSpec.describe "Leys", type: :request do
  describe "GET /leys" do
    xit "works! (now write some real specs)" do
      get leys_path
      expect(response).to have_http_status(200)
    end
  end
end
